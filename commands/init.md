---
description: Initialize project configuration for aet feature management. Creates .aet/config.json with repository information (upstream/fork) and configures platform token in global config (~/.aet/config.json). Use when setting up a new project or when configuration is missing.
disable-model-invocation: true
---

Automatically detect the language of user input and respond in the same language.

Invoke the aet-setup-config skill and follow it exactly as presented to you