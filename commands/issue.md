---
description: Issue management for creating, updating, claiming and tracking platform issues. Use natural language to create issues, update issues, claim issues, or query issue details.
disable-model-invocation: true
---

Automatically detect the language of user input and respond in the same language.

Invoke the `aet-operating-issues` skill with the provided natural language arguments, then execute exactly as the skill presents.